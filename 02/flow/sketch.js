function setup() {
  createCanvas(500, 500);
  let x = 100;

  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
  x = x + 1;
  ellipse(x, 100, 50, 50);
}
